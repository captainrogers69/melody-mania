enum KButtonState { idle, processing, error, success }

enum KButtonType {
  whiteButton,
  primaryButton,
  successButton,
  errorButton,
  disabledButton
}

/// circular = 31, rounded = 15, mid = 10, standard = 5, default = 2,
enum KButtonCorner { circular, rounded, standard, mid }
